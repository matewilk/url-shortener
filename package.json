{
  "name": "url-shortener",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "packageManager": "npm@10.8.3",
  "devDependencies": {
    "turbo": "^2.1.1",
    "vitest": "^2.0.5"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "zod": "^3.23.8"
  }
}
