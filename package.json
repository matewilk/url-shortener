{
  "name": "url-shortener",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "packageManager": "npm@10.0.0",
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "zod": "^3.23.8"
  }
}
